machine:
  node:
    version: 7.10.0
general:
  artifacts:
    - coverage
dependencies:
  pre:
    - npm install -g codecov
test:
  override:
    - nvm use 7.10.0
    - npm run build
    - codecov -t $CODECOV_TOKEN || echo 'Sending coverage failed'
    - nvm use 4.0 && npm run test-dist
